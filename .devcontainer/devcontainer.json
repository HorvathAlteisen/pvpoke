{
    "name": "pvpoke",
    "dockerComposeFile": "../docker/docker-compose.yml",
    "service": "web",
    "workspaceFolder": "/var/www/html/pvpoke/src",
    "onCreateCommand": "sh -c 'apt-get update && apt-get install git -y'"
}